services:
  - type: worker
    name: checkin-scraper
    env: python
    plan: starter

    envVars:
      PYTHON_VERSION: 3.11
      PLAYWRIGHT_BROWSERS_PATH: "/opt/render/project/.pw-browsers"

    buildCommand: |
      mkdir -p logs
      mkdir -p /opt/render/project/.pw-browsers
      pip install -r requirements.txt
      PLAYWRIGHT_BROWSERS_PATH=/opt/render/project/.pw-browsers playwright install chromium

    startCommand: |
      python main_checkin.py
